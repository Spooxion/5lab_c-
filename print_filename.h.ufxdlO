#ifndef PRINT_FILENAME_H
#define PRINT_FILENAME_H

#include <QDialog>

namespace Ui {
class print_filename;
}

class print_filename : public QDialog
{
    Q_OBJECT

public:
    explicit print_filename(QWidget *parent = nullptr);
    ~print_filename();

private:
    Ui::print_filename *ui;
};

#endif // PRINT_FILENAME_H
